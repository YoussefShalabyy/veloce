const domain = "13.61.32.37:8000/";
const endpoints = {
  login: `http://${domain}/user/login/`,
  register: `http://${domain}/user/register/`,
  tickets: `http://${domain}/ticket/`,
};

export default endpoints;
